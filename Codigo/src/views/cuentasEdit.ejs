<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../css/cuentas.css">
    <%- include('partial/head') %>
    <title>Edicion de cuentas</title>
</head>
<body >
    <%- include('partial/header') %>
    <main>
        <p class="p-user">Usuario a editar: <%= user.nombre %></p>
        <form action="/cuentas/details/<%= user.id %>?_method=PUT" method="post" class="form-user">
            <div>
                <label for="email">Email del usuario</label>
                <br>
                <input type="email"name="email"value="<%= user.email %>">
            </div>
            <div>
                <label for="rol">1. adminitrador<br>0. usuario</label>
                <br>
                <input type="number" min="0" max="1" name="rol"
                <% if(user.rol!= null) { %>
                    value="<%= user.rol %>"
                <% }else{ %>
                    value="0"
                <% } %>
                >
            </div>
            <div>
                <input type="submit" value="editar">
            </div>
            
        </form>
    </main>
    <%- include('partial/footer') %>
</body>
</html>